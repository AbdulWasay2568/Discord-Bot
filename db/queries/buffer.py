# H. Sync / Buffer State

# Endpoints for tracking sync / backfill state.

# GET /sync_state/{guild_id}/{channel_id} – Get last synced message id, timestamps.

# POST /sync_state – Create / update sync state.

# PUT /sync_state/{guild_id}/{channel_id} – Update sync state after reconciliation/backfill.
